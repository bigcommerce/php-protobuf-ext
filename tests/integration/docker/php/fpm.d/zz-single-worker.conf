; Pin the pool to a single, long-lived worker so descriptor pools persist
; across requests deterministically (otherwise requests may hit fresh workers
; and the cross-request behavior under test never materializes).
[www]
pm = static
pm.max_children = 1
pm.max_requests = 100000
